{
  "private": true,
  "scripts": {
    "postinstall": "spago install",
    "clean": "rm -rf output .spago dist/*",
    "build": "spago build",
    "test": "spago test"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "pulp": "^8.1.1",
    "purescript": "^0.13.6",
    "spago": "^0.14.0"
  }
}
